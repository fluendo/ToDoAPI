<Window x:Class="FluentToDo.WPF.AddTodoItemView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FluentToDo.WPF"
        mc:Ignorable="d"
        Title="AddTodoItemView" Height="150" Width="300">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Name"/>
            <TextBox Text="{Binding ItemName}" Height="30" VerticalAlignment="Top"/>
        </StackPanel>
        
        <Button VerticalAlignment="Bottom" Command="{Binding CommandSave}">
            <TextBlock Text="Save"/>
        </Button>
    </Grid>
</Window>
